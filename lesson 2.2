public class Main {
    
    // первый метод
    public static void printThreeWords() {
        System.out.println("Orange");
        System.out.println("Banana");
        System.out.println("Apple");
    }
    
    // второй метод
    public static void checkSumSign() {
      int a = -1;
      int b = -2;
      
      int sum = a + b;
      
      if (sum >= 0) {
        System.out.println("Сумма положительная");
      } 
      else {
        System.out.println("Сумма отрицательная");
      }
    }
    
    // третий метод
    public static void printColor() {
      int value = 12222;
      if (value < 0) {
          System.out.println("Красный");
        } else if (value <= 100) {
          System.out.println("Желтый");
        } else {
          System.out.println("Зеленый");
        }
    }
    // четвертый метод
    public static void compareNumbers() {
      int a = -1;
      int b = 1;
      
      if (a >= b) {
        System.out.println ("a >= b");
      }
      else {
        System.out.println ("a < b");
      }
    }
    // пятый метод
    public static Boolean fiveMethod (int a, int b) {
      
      int sum = a + b;
      
      if (sum >= 10 && sum <= 20) {
        return true;
        } 
        else {
        return false;
        }
    }
    // шестой метод
    public static void sixMethod (int number) {
      if (number >= 0 ) {
        System.out.println("Положительное");
      }
      else {
        System.out.println("Отрицательное");
      }
    }
    
    // седьмой метод
    public static Boolean sevenMethod (int number) {
     if (number >= 0 ) {
        return true;
      }
      else {
        return false;
      }
    }
    // восьмой метод
    public static void eightMethod (String text, int count) {
      for (int i = 0; i < count; i++) {
        System.out.println(text);
      }
    }
    
    // девятый метод
    public static Boolean nineMethod (int year) {
      if (year % 400 == 0) {
        return true;
      }
      if (year % 100 == 0) {
        return false;
      }
      if (year % 4 == 0) {
        return true;
      }
      return false;
    }
    
    // десятый метод
    public static void tenMethod () {
      int[] mass = {1, 1, 0, 0, 1, 0, 1, 1, 0, 0};
      
        System.out.println("Было:");
      for (int i = 0; i < mass.length; i++) {
        System.out.print(mass[i] + " ");
      }
      
    System.out.println();
    
      for (int i = 0; i < mass.length; i++) {
        if (mass[i] == 0) {
            mass[i] = 1;
        } else {
            mass[i] = 0;
        }
    }
    
    System.out.println("Стало:");
      for (int i = 0; i < mass.length; i++) {
        System.out.print(mass[i] + " ");
      }
              System.out.println();
    }
      
    //Одиннадцатый метод
    public static void elevenMethod () {
      int [] numbers = new int [100];
      
      for (int i =0; i < numbers.length; i++) {
        numbers [i] = i + 1;
            System.out.println(numbers[i] + " ");
      }
      
    }
    
    // Двенадцатый метод
        public static void twelveMethod () {
          int [] numbers = {1, 5, 3, 2, 11, 4, 5, 2, 4, 8, 9, 1};
            
            System.out.println("Было:");
            for (int i = 0; i < numbers.length; i++) {
              System.out.print(numbers[i] + " ");
            }
        System.out.println();  
        
          for (int i = 0; i < numbers.length; i++) {
            if (numbers[i] < 6) {
            numbers[i] = numbers[i] * 2;
            }
          } 
        
           System.out.println("Стало:");
          for (int i = 0; i < numbers.length; i++) {
            System.out.print(numbers[i] + " ");
          }
                  System.out.println();
        }
      
      //Тринадцатый метод
        public static void thirteenMethod () {
            int[][] table = new int[3][3];

          for (int i = 0; i < 3; i++) {
            table[i][i] = 1;
          }
          for (int i = 0; i < 3; i++) {
            for (int j = 0; j < 3; j++) {
              System.out.print(table[i][j] + " ");
            }
            System.out.println();
          }
        }

        //Четырнадцатый метод
        public static int [] fourteen (int len, int initialValue) {
          int[] arr = new int[len];
          
          for (int i = 0; i < len; i++) {
            arr[i] = initialValue;
          }
           
          for (int i = 0; i < arr.length; i++) {
           System.out.print(arr[i] + " ");
          }
            System.out.println();
            
             return arr;
        }
    
    // главный метод
   public static void main(String[] args) {
        printThreeWords();
        System.out.println();
        checkSumSign();
        System.out.println();
        printColor();
        System.out.println();
        compareNumbers();
        System.out.println();
        System.out.println(fiveMethod(5, 7));
        System.out.println(fiveMethod(-5, 7));
        System.out.println(fiveMethod(0, 10));
        System.out.println();
        sixMethod (0);
        sixMethod (-1);
        System.out.println();
        System.out.println(sevenMethod(0));
        System.out.println(sevenMethod(-1));
        System.out.println();
        eightMethod("Мама я в телевизоре!", 4);
        System.out.println();
        System.out.println(nineMethod(2024));
        System.out.println(nineMethod(1901));
        System.out.println(nineMethod(2004)); 
        System.out.println(nineMethod(2023));
        System.out.println();
        tenMethod();
        System.out.println();
        elevenMethod();
        System.out.println();
        twelveMethod();
        System.out.println();
        thirteenMethod();
        System.out.println();
        fourteen(10, 5);
    }
    
}
